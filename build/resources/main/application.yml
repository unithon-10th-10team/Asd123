spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://se-community-db.cj1usy9eqvio.ap-northeast-2.rds.amazonaws.com:3306/se_community_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: selabhh1234

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: none
      use-new-id-generator-mappings: false
    show-sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect

jwt:
  token:
    secret-key: 'RoqUcfvBYnu90fx8Q2MeRYuw4AcdOxXQtc2lsdmVybmluZS'
    expiry: 1800000
    refresh-expiry: 3600000

springdoc:
  packages-to-scan: com.core.service
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    operations-sorter: method
    display-request-duration: true
    tags-sorter: alpha
  auto-tag-classes: true

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always

logging:
  level:
    org.hibernate.SQL: debug

kakao:
  client:
    registration:
        client-id: 4fc99ed678f3d2fa5db774b0387bc601
        redirect-uri: http://localhost:8080/api/v1/oauth2/kakao
        client-authentication-method: POST
        authorization-grant-type: authorization_code
        scope: profile_nickname, account_email, gender, birthday
    provider:
        authorization-uri: https://kauth.kakao.com/oauth/authorize
        token-uri: https://kauth.kakao.com/oauth/token
        user-info-uri: https://kapi.kakao.com/v2/user/me
        user-name-attribute: id

cors:
  allowed-origins: 'http://localhost:3000/'
  allowed-methods: GET,POST,PATCH,PUT,DELETE,OPTIONS
  allowed-headers: '*'
  max-age: 3600